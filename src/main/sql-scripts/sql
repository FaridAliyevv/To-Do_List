CREATE DATABASE todo_app;
use todo_app;

CREATE TABLE tasks (
    id SERIAL PRIMARY KEY,
    task TEXT NOT NULL,
    priority TEXT NOT NULL
);

ALTER TABLE tasks
ADD COLUMN description TEXT;

select * from tasks;